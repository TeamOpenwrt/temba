# README

This is a rails application that allows user to download a firmware based on some parameters in a form

This application at the moment does not use a database, but there is a model `@node` to perform validations in the form

## Run server

    rails s
